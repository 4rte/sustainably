<div class="gradient-container" style="height: 100vh">
  <%= render 'shared/navbar' %>
  <h2 style="font-size: 28px; margin-top: 4vh;"> what are you looking for?</h2>
    <img src="<%= asset_path('logo.png') %>" alt="logo-sustainably" style="width: 70px; height: 70px;margin-bottom: 3vh;">
    <% @categories.each do |category| %>
      <div class="card-category" style="background-size: cover; background-image: url(<%= asset_path category.asset_path %>)"><div class="btn-category-black"> <%= link_to category.title, category_path(category) %> </div></div>
    <% end %>
</div>

